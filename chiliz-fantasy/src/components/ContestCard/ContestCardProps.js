// ContestCardProps.js
export default class ContestCardProps {
    constructor(contestId, matchId, prizePool, spotsRemaining, spotsTotal) {
      this.contestId = contestId;
      this.matchId = matchId;
      this.prizePool = prizePool;
      this.spotsRemaining = spotsRemaining;
      this.spotsTotal = spotsTotal;
    }
  }
  